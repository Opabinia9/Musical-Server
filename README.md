I made this for a code challenge, it works with the code being run on devices on the same network or the same device, 
just replace localhost with the apropriate ip adress
you also need to add the path to a file with a list of line sepperated full file path to the songs
